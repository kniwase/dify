# Please do not directly edit this file. Instead, modify the .env variables related to NGINX configuration.

server {
    listen ${NGINX_PORT};
    server_name ${NGINX_SERVER_NAME};

    location /self-hosted-dify/console/api {
      proxy_pass http://api:5001/console/api;
      include proxy.conf;
    }

    location /self-hosted-dify/api {
      proxy_pass http://api:5001/api;
      include proxy.conf;
    }

    location /self-hosted-dify/v1 {
      proxy_pass http://api:5001/v1;
      include proxy.conf;
    }

    location /self-hosted-dify/files {
      proxy_pass http://api:5001/files;
      include proxy.conf;
    }

    location /self-hosted-dify/explore {
      proxy_pass http://web:3000/self-hosted-dify/explore;
      include proxy.conf;
    }

    location /self-hosted-dify/e/ {
      proxy_pass http://plugin_daemon:5002;
      proxy_set_header Dify-Hook-Url $scheme://$host$request_uri;
      include proxy.conf;
    }

    location /self-hosted-dify/ {
      proxy_pass http://web:3000/self-hosted-dify/;
      include proxy.conf;
    }

    # placeholder for acme challenge location
    ${ACME_CHALLENGE_LOCATION}

    # placeholder for https config defined in https.conf.template
    ${HTTPS_CONFIG}
}
